profiles:
  # ---------------------------------------------------------------------------
  # IBM DPI (ex. "IBM DPI C19 PDU+", firmware V02xx, arbre Powerware 534)
  # Sources : MIB IBM_DPI_0_91_Linux.mib + snmpwalk du PDU réel.
  # PDU SURVEILLÉE : mesures globales + par prise, prises NON commutables via SNMP.
  # Le nombre de prises est AUTO-DÉTECTÉ en sondant les load groups (l'OID
  # pduDeviceOutputLineNumber renvoie le nombre de lignes/phases, pas de prises).
  # ---------------------------------------------------------------------------
  IBM-DPI:
    description: "IBM DPI — PDU surveillée (mesures), prises non commutables via SNMP"
    switched: false
<<<<<<< HEAD
    estimate_power_if_zero: true   # firmware DPI : TruePower parfois à 0 sous charge -> estimation V×I
=======
>>>>>>> 2b92c410788cd2d492efd5578c82d6a1c11d4499
    metrics:
      # pduOutputVoltageAPresentValue (0.1 V) — walk: 2362 -> 236.2 V
      voltage:     { oid: "1.3.6.1.4.1.534.6.6.2.1.3.2.7.1.1.0",  scale: 0.1 }
      # pduOutputCurrentAPresentValue (0.1 A) — index .13 (les .10-.12 sont la fréquence !)
      current:     { oid: "1.3.6.1.4.1.534.6.6.2.1.3.2.7.1.13.0", scale: 0.1 }
      # pduDeviceOutputTotalPower (Watts)
      power:       { oid: "1.3.6.1.4.1.534.6.6.2.1.3.2.5.0",      scale: 1 }
      # temperature : volontairement omise (le capteur ambiant renvoie 0 sur ce firmware)
    outlets:
      style: dpi
      max_groups: 12          # sondés au maximum ; le nombre réel est auto-détecté
      # Nom de la prise J{i} : {name_base}.{i+1}.0  ("[description]" = non renseigné)
      name_base:  "1.3.6.1.4.1.534.6.6.2.1.3.2.8"
      # Load group i : {group_base}.{i+1}.<obj>.0
      #   .1  = tension présente (0.1 V) — sert de sonde d'existence du groupe
      #   .4  = courant présent (0.1 A)
      #   .10 = puissance réelle présente (Watts)
      group_base: "1.3.6.1.4.1.534.6.6.2.1.3.2.7"

  # Ancien profil (arbre IBM 1.3.6.1.4.1.2.6.223.8), conservé pour référence.
  IBM-42R8743:
    description: "Profil historique (arbre IBM) — commutable si le firmware l'expose"
    switched: true
    mib_root: "1.3.6.1.4.1.2.6.223.8"
    outlet_count_oid: "1.3.6.1.4.1.2.6.223.8.2.1.0"
    metrics: {}
    outlets:
      style: table
      table_base: "1.3.6.1.4.1.2.6.223.8.2.2.1"
      name:   "1.3.6.1.4.1.2.6.223.8.2.2.1.2"
      state:  "1.3.6.1.4.1.2.6.223.8.2.2.1.13"
